# Rime dict
# encoding: utf-8

---
name: holo
version: "0.2"
use_preset_vocabulary: false
sort: original
columns:
  - text
  - code
import_tables:
  - holo.abbreviation
  - holo.full
encoder:
  exclude_patterns:
    - "^[a-z]$"
    - "[^a-z]"
  rules:
    - length_equal: 2
      formula: "AaAbAcBaBbBc" # 2 + 2
    - length_equal: 3
      formula: "AaAbAcBaBbBcCaCbCc" # 2 + 2 + 2
    - length_in_range: [4, 99]
      formula: "AaAbAcBaBbBcCaCbCcDaDbDcDdZaZb" # 2 + 2 + 2 + 末2

---
